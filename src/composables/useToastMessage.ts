export const showToastMessage = ({ status, message }: { status: string; message: string }) => {
  switch (status) {
    case 'success':
        
      break;
    case 'error':
      break;
    case 'warning':
      break;
    case 'info':
      break;
  }
};
